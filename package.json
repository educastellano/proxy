{
  "name": "@educastellano/proxy",
  "version": "1.0.0",
  "description": "A proxy to bind your apps to an API",
  "main": "index.js",
  "bin": {
    "proxy": "./cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "minimatch": "^3.0.2",
    "minimist": "^1.2.0",
    "node-emoji": "^1.3.1",
    "node-static": "^0.7.7",
    "request": "^2.72.0"
  }
}
